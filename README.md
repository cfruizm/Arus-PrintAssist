# Arus PrintAssist

Prototype of a domain-specific chatbot for first-level print support.

## Main capabilities
- documentation-oriented question answering
- retrieval-augmented generation (RAG)
- conversational memory
- structured escalation support
- persistent logging and incident export

## Local run
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py

