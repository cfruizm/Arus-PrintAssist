# Apply these three integration edits to the current lab_session.py.
# 1. Import:
from .documented_router import maybe_generate_procedural

# 2. Immediately after the existing _generate_documented(...) call in BOTH
#    the exact-cache branch and normal branch:
result, procedural_trace = maybe_generate_procedural(
    result, message, _gateway(secrets, s), budget, store,
    str(getattr(load_gateway_config(secrets), "model", "") or ""),
)
result.setdefault("provider_trace", {})["procedural_answer"] = (
    procedural_trace or {"skipped": True, "reason": "procedural_answer_not_called"}
)
if procedural_trace and not procedural_trace.get("skipped"):
    add_result(store["telemetry"], procedural_trace)

# 3. Include "procedural_answer" in _artifact(...) and initialize:
#    store.setdefault("procedural_answer_cache", {})
#    cache_metrics.setdefault("procedural_answer_hits", 0)
